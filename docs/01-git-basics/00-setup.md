# Setup Git

## Installation steps
1. click https://www.git-scm.com/
2. click Downloads
3. click download for `{your os}` and install it.

## For Ubuntu
```sh
sudo add-apt-repository ppa:git-core/ppa
sudo apt update
sudo apt install git
git --version
```

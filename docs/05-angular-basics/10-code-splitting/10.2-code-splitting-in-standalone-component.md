# Code Splitting In Standalone Component

## Project Metadata

- The project below was created using angular cli 21.x and uses tailwindcss for styling
- repository: https://github.com/neutral-00/practice-angular
- parent branch: main
- branch: 10.2-code-splitting-in-standalone-component

## Learning Objective

1. Learn code splitting by lazy loading a standalone component

## Scenario

- In the existing project we will add a new component named `about` via the command `ng g c components/about --type=component`.
- Then we lazy load it in the root routing.

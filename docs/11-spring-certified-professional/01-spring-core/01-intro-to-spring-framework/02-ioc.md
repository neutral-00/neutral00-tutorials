# Inversion of Control (IoC)

Inversion of Control in Spring is the principle of delegating creation and wiring of application objects to the framework (the container) instead of letting application code construct dependencies directly.

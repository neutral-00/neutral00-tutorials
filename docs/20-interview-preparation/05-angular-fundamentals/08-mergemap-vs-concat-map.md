# MergeMap Vs ConcatMap

- They are RxJs operators
- `mergeMap` allows multiple inner observables to run concurrently.
- `concatMap` processes inner observables sequentially, waiting for each to complete before starting the next.



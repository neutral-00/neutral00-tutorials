# Lead Angular Developer - Real Interview Questions

- 8+ yrs

## Source

- Chetan Jogi via LinkedIn Post

## L1 Angular Questions

1. `setValue()` vs `patchValue()` in reactive forms
1. `reset()` method of forms and when to use them
1. using the same object with `setValue()` and `resetValue()` what happens internally
1. what are smart and dumb components?
1. create a re-usable textbox component with `ControlValueAccessor`
1. treeshaking in angular, how it works and why it matters?
1. commonly used RxJs operator with real world usage
1. on logout, how to send user activity to an api
1. how to handle dynamic menu as per role

## L1 Javascript Questions

1. describe some important ES6 features used in angular project
1. describe one use case of `Promise.race()`
1. describe one use case of object destructuring
1. describe shallow copy and deep copy
1. what is hoisting?
1. what is closure? explain with one use case.
